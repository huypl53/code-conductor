---
phase: 11-packaging-and-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - docs/GETTING-STARTED.md
autonomous: false
requirements:
  - PKG-04

must_haves:
  truths:
    - "A developer who has never seen the project can follow the guide to run a multi-agent session"
    - "The guide covers prerequisites, installation, API key config, first CLI session, and optional dashboard"
    - "Both pip and npm install commands match the actual package names"
  artifacts:
    - path: "docs/GETTING-STARTED.md"
      provides: "Complete getting-started guide from zero to first session"
      min_lines: 50
      contains: "pip install conductor-ai"
  key_links:
    - from: "docs/GETTING-STARTED.md"
      to: "packages/conductor-core/pyproject.toml"
      via: "install command references correct package name"
      pattern: "pip install conductor-ai"
    - from: "docs/GETTING-STARTED.md"
      to: "packages/conductor-dashboard/package.json"
      via: "install command references correct package name"
      pattern: "npm install.*conductor-dashboard"
---

<objective>
Create the getting-started guide and verify both packages install cleanly.

Purpose: A new developer with no prior knowledge of Conductor can follow this guide to install the tools and run their first multi-agent coding session. This is the final deliverable for v1 packaging.
Output: docs/GETTING-STARTED.md and verified clean installs of both packages.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-packaging-and-distribution/11-RESEARCH.md
@.planning/phases/11-packaging-and-distribution/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getting-started guide</name>
  <files>docs/GETTING-STARTED.md</files>
  <action>
Create `docs/GETTING-STARTED.md` with the following sections:

1. **Title and intro:** "Getting Started with Conductor" - one paragraph explaining what Conductor does (AI agent orchestration for coding tasks).

2. **Prerequisites:**
   - Python 3.12+
   - Node.js 22+ (optional, for web dashboard)
   - An Anthropic API key (link to console.anthropic.com)

3. **Installation:**
   ```bash
   pip install conductor-ai
   # Optional: web dashboard
   npm install -g conductor-dashboard
   ```

4. **Configuration:**
   - Set `ANTHROPIC_API_KEY` environment variable
   - Show both export command and .env file approach
   - Note: Conductor reads the key from environment automatically

5. **Your First Session (CLI):**
   - `cd` into a project directory (any repo with code)
   - Run: `conductor run "Add a hello world endpoint to the API" --auto`
   - Explain what happens: orchestrator decomposes, spawns agents, monitors, reviews
   - Show expected terminal output (agent status table)

6. **Your First Session (Interactive):**
   - Run without --auto: `conductor run "Add dark mode to settings"`
   - Explain: orchestrator asks clarifying questions, user can intervene
   - Show intervention commands: cancel, feedback, redirect

7. **Using the Web Dashboard (Optional):**
   - Start with dashboard: `conductor run "feature description" --auto --dashboard-port 8000`
   - In another terminal: `conductor-dashboard 4173`
   - Open http://localhost:4173
   - Explain: agent cards, expandable detail, live stream, intervention controls

8. **Project Configuration (Optional):**
   - Mention `.claude/` directory and `CLAUDE.md` for project-specific context
   - Mention `.memory/` for cross-agent knowledge persistence

9. **Troubleshooting:**
   - "conductor: command not found" -> ensure pip bin directory is in PATH
   - "ANTHROPIC_API_KEY not set" -> set the environment variable
   - "Dashboard not connecting" -> ensure --dashboard-port matches and backend is running

10. **Next Steps:**
    - Link to README for architecture overview
    - Mention --auto vs interactive modes
  </action>
  <verify>
    <automated>test -f /home/huypham/code/digest/claude-auto/docs/GETTING-STARTED.md && wc -l /home/huypham/code/digest/claude-auto/docs/GETTING-STARTED.md | awk '{print ($1 >= 50) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>docs/GETTING-STARTED.md exists with 50+ lines covering prerequisites, installation, configuration, first CLI session, interactive mode, dashboard usage, project config, and troubleshooting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify packaging and guide</name>
  <action>
Human verifies complete packaging configuration for both packages (Plan 01) and getting-started guide (this plan):
- Python package: conductor-ai with PyPI metadata, README, CLI entry point
- npm package: conductor-dashboard with bin script, sirv server, README
- Getting-started guide: docs/GETTING-STARTED.md
- MIT LICENSE at repo root
  </action>
  <verify>
1. Verify Python package builds:
   ```bash
   cd packages/conductor-core && uv build
   ```
   Expect: `conductor_ai-0.1.0-py3-none-any.whl` in dist/

2. Verify npm package packs:
   ```bash
   cd packages/conductor-dashboard && pnpm build && npm pack --dry-run
   ```
   Expect: tarball listing includes bin/ and dist/

3. Review docs/GETTING-STARTED.md for clarity and completeness

4. Verify LICENSE file exists at repo root
  </verify>
  <done>Human confirms both packages build correctly and getting-started guide is clear and complete.</done>
</task>

</tasks>

<verification>
- docs/GETTING-STARTED.md exists with all required sections
- Guide references correct package names (conductor-ai, conductor-dashboard)
- Guide includes prerequisites, install, configure, first session, dashboard, troubleshooting
- Human has verified both packages build correctly
</verification>

<success_criteria>
A developer who has never seen the project can follow docs/GETTING-STARTED.md from zero to running their first multi-agent session. Both packages build without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/11-packaging-and-distribution/11-02-SUMMARY.md`
</output>
