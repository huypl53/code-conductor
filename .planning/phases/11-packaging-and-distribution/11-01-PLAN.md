---
phase: 11-packaging-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/conductor-core/pyproject.toml
  - packages/conductor-core/README.md
  - packages/conductor-dashboard/package.json
  - packages/conductor-dashboard/bin/conductor-dashboard.js
  - packages/conductor-dashboard/README.md
  - LICENSE
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "Python package builds as conductor-ai with correct metadata and CLI entry point"
    - "npm package is public with bin entry that serves built dashboard via sirv"
    - "Both packages include LICENSE and README files"
  artifacts:
    - path: "packages/conductor-core/pyproject.toml"
      provides: "PyPI metadata: name=conductor-ai, classifiers, description, license, readme"
      contains: 'name = "conductor-ai"'
    - path: "packages/conductor-core/README.md"
      provides: "PyPI long description"
      min_lines: 20
    - path: "packages/conductor-dashboard/package.json"
      provides: "npm package config: no private flag, bin entry, files array, prepublishOnly"
      contains: '"conductor-dashboard"'
    - path: "packages/conductor-dashboard/bin/conductor-dashboard.js"
      provides: "CLI bin script that serves dist/ via sirv with SPA fallback"
      min_lines: 10
    - path: "packages/conductor-dashboard/README.md"
      provides: "npm package page description"
      min_lines: 15
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
  key_links:
    - from: "packages/conductor-core/pyproject.toml"
      to: "packages/conductor-core/src/conductor/__init__.py"
      via: "version and package name alignment"
      pattern: 'version = "0.1.0"'
    - from: "packages/conductor-dashboard/package.json"
      to: "packages/conductor-dashboard/bin/conductor-dashboard.js"
      via: "bin field references bin script"
      pattern: '"bin/conductor-dashboard.js"'
---

<objective>
Configure both packages for public distribution: conductor-ai on PyPI and conductor-dashboard on npm.

Purpose: Transform the monorepo's two packages from private development projects into publishable, installable packages with proper metadata, READMEs, license, and bin scripts.
Output: Both packages ready to build and publish. `uv build` produces a correct wheel; `npm pack` produces a correct tarball.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-packaging-and-distribution/11-RESEARCH.md

@packages/conductor-core/pyproject.toml
@packages/conductor-core/src/conductor/__init__.py
@packages/conductor-dashboard/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python package PyPI metadata + LICENSE + README</name>
  <files>packages/conductor-core/pyproject.toml, packages/conductor-core/README.md, LICENSE</files>
  <action>
1. Update `packages/conductor-core/pyproject.toml`:
   - Change `name = "conductor-core"` to `name = "conductor-ai"`
   - Add `description = "AI agent orchestration - a self-organizing team of coding agents"`
   - Add `readme = "README.md"`
   - Add `license = "MIT"`
   - Add `requires-python = ">=3.12"` (already present, keep)
   - Add `authors = [{ name = "Conductor Team" }]`
   - Add classifiers:
     ```
     classifiers = [
         "Development Status :: 4 - Beta",
         "Intended Audience :: Developers",
         "License :: OSI Approved :: MIT License",
         "Programming Language :: Python :: 3",
         "Programming Language :: Python :: 3.12",
         "Programming Language :: Python :: 3.13",
         "Topic :: Software Development :: Libraries",
     ]
     ```
   - Keep all existing fields (dependencies, scripts, build-system, tool configs) unchanged

2. Create `LICENSE` at repo root with MIT License text, year 2026, holder "Conductor Contributors"

3. Create `packages/conductor-core/README.md` with:
   - Package name and one-line description
   - What it does (orchestrator, ACP communication, state management, CLI)
   - Installation: `pip install conductor-ai`
   - Quick usage: `conductor run "description" --auto`
   - Note that import path is `conductor` (not `conductor-ai`)
   - Link to getting-started guide (docs/GETTING-STARTED.md)
   - License section referencing MIT
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv build 2>&1 | tail -5</automated>
  </verify>
  <done>pyproject.toml has conductor-ai name with full PyPI metadata. LICENSE exists at repo root. README.md exists in conductor-core with install and usage instructions. `uv build` succeeds producing a wheel named conductor_ai-0.1.0-*.whl.</done>
</task>

<task type="auto">
  <name>Task 2: npm package distribution config + bin script + README</name>
  <files>packages/conductor-dashboard/package.json, packages/conductor-dashboard/bin/conductor-dashboard.js, packages/conductor-dashboard/README.md</files>
  <action>
1. Update `packages/conductor-dashboard/package.json`:
   - Remove `"private": true`
   - Add `"description": "Web dashboard for Conductor AI agent orchestration"`
   - Add `"license": "MIT"`
   - Add `"bin"` field: `{ "conductor-dashboard": "bin/conductor-dashboard.js" }`
   - Add `"files"` field: `["dist/", "bin/"]`
   - Add to scripts: `"prepublishOnly": "npm run build"`
   - Add `sirv` (not sirv-cli) as a production dependency: `"sirv": "^3.0.0"`
   - Keep all existing fields unchanged

2. Create `packages/conductor-dashboard/bin/conductor-dashboard.js`:
   ```javascript
   #!/usr/bin/env node
   import sirv from 'sirv';
   import { createServer } from 'node:http';
   import { fileURLToPath } from 'node:url';
   import { dirname, join } from 'node:path';

   const __dirname = dirname(fileURLToPath(import.meta.url));
   const distDir = join(__dirname, '..', 'dist');
   const port = parseInt(process.argv[2] || '4173', 10);

   const handler = sirv(distDir, { single: true });
   createServer(handler).listen(port, () => {
     console.log(`Conductor Dashboard: http://localhost:${port}`);
   });
   ```
   Make the file executable (chmod +x).

3. Create `packages/conductor-dashboard/README.md` with:
   - Package name and one-line description
   - What it provides (web UI for monitoring agents, live stream, intervention controls)
   - Installation: `npm install -g conductor-dashboard`
   - Usage: `conductor-dashboard [port]` (default 4173)
   - Note: requires the Python conductor-ai backend running with `--dashboard-port`
   - License section referencing MIT
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-dashboard && pnpm install && npm pack --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>package.json has no private flag, has bin/files/prepublishOnly/sirv dependency. bin/conductor-dashboard.js exists and is executable. README.md exists with install and usage instructions. `npm pack --dry-run` lists dist/ and bin/ in the tarball contents.</done>
</task>

</tasks>

<verification>
- `uv build` in conductor-core produces conductor_ai-0.1.0-py3-none-any.whl
- `npm pack --dry-run` in conductor-dashboard lists bin/ and dist/ entries
- LICENSE file exists at repo root
- Both README.md files exist with installation instructions
</verification>

<success_criteria>
Both packages are configured for public distribution with correct metadata, license, READMEs, and (for npm) a bin script. Building produces correctly named artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-packaging-and-distribution/11-01-SUMMARY.md`
</output>
