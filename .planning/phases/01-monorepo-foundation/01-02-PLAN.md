---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/conductor-dashboard/package.json
  - packages/conductor-dashboard/tsconfig.json
  - packages/conductor-dashboard/tsconfig.app.json
  - packages/conductor-dashboard/tsconfig.node.json
  - packages/conductor-dashboard/vite.config.ts
  - packages/conductor-dashboard/biome.json
  - packages/conductor-dashboard/index.html
  - packages/conductor-dashboard/src/main.tsx
  - packages/conductor-dashboard/src/App.tsx
  - packages/conductor-dashboard/src/index.css
  - packages/conductor-dashboard/src/vite-env.d.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PKG-03

must_haves:
  truths:
    - "pnpm install at repo root installs dashboard dependencies without errors"
    - "Dashboard dev server starts without errors (vite)"
    - "Biome check passes on dashboard source"
    - "TypeScript strict check passes on dashboard source"
    - "CI workflow defines parallel Python and Dashboard jobs"
  artifacts:
    - path: "packages/conductor-dashboard/package.json"
      provides: "Dashboard package config"
      contains: "conductor-dashboard"
    - path: "packages/conductor-dashboard/vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins"
      contains: "@tailwindcss/vite"
    - path: "packages/conductor-dashboard/biome.json"
      provides: "Biome linting and formatting config"
      contains: "recommended"
    - path: "packages/conductor-dashboard/src/App.tsx"
      provides: "Root React component"
      contains: "function App"
    - path: "packages/conductor-dashboard/src/index.css"
      provides: "Tailwind v4 CSS entry"
      contains: '@import "tailwindcss"'
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with parallel Python and Dashboard jobs"
      contains: "python:"
  key_links:
    - from: "packages/conductor-dashboard/vite.config.ts"
      to: "packages/conductor-dashboard/src/index.css"
      via: "Tailwind v4 Vite plugin processes CSS"
      pattern: "tailwindcss"
    - from: ".github/workflows/ci.yml"
      to: "packages/conductor-core"
      via: "working-directory in Python job steps"
      pattern: "packages/conductor-core"
    - from: ".github/workflows/ci.yml"
      to: "packages/conductor-dashboard"
      via: "pnpm --filter conductor-dashboard"
      pattern: "conductor-dashboard"
---

<objective>
Create the Node.js dashboard scaffold (Vite + React + Tailwind v4 + TypeScript + Biome) and the GitHub Actions CI workflow.

Purpose: Complete the monorepo by adding the frontend package and CI, so both halves of the monorepo are functional and quality-gated on every push.
Output: Working dashboard scaffold with passing Biome/TypeScript checks, and a CI workflow with parallel Python + Dashboard jobs.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold dashboard package with Vite, React, Tailwind v4, TypeScript, and Biome</name>
  <files>
    packages/conductor-dashboard/package.json,
    packages/conductor-dashboard/tsconfig.json,
    packages/conductor-dashboard/tsconfig.app.json,
    packages/conductor-dashboard/tsconfig.node.json,
    packages/conductor-dashboard/vite.config.ts,
    packages/conductor-dashboard/biome.json,
    packages/conductor-dashboard/index.html,
    packages/conductor-dashboard/src/main.tsx,
    packages/conductor-dashboard/src/App.tsx,
    packages/conductor-dashboard/src/index.css,
    packages/conductor-dashboard/src/vite-env.d.ts
  </files>
  <action>
Scaffold the dashboard package. Do NOT use `pnpm create vite` interactively -- create files manually based on the Vite react-ts template structure.

1. `packages/conductor-dashboard/package.json`:
   - `name: "conductor-dashboard"`, `private: true`, `version: "0.1.0"`, `type: "module"`
   - `scripts`: `dev: "vite"`, `build: "tsc -b && vite build"`, `preview: "vite preview"`
   - `dependencies`: `react`, `react-dom` (latest 19.x)
   - `devDependencies`: `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `typescript` (5.7+), `vite` (6+), `@tailwindcss/vite` (4+), `tailwindcss` (4+), `@biomejs/biome` (1.9+)
   - Use `latest` or current stable versions -- check with `pnpm info react version` etc. at runtime

2. `packages/conductor-dashboard/tsconfig.json` — project references:
   ```json
   {
     "files": [],
     "references": [
       { "path": "./tsconfig.app.json" },
       { "path": "./tsconfig.node.json" }
     ]
   }
   ```

3. `packages/conductor-dashboard/tsconfig.app.json` — app TypeScript config:
   - `compilerOptions`: `target: "ES2020"`, `useDefineForClassFields: true`, `lib: ["ES2020", "DOM", "DOM.Iterable"]`, `module: "ESNext"`, `skipLibCheck: true`, `moduleResolution: "bundler"`, `allowImportingTsExtensions: true`, `isolatedModules: true`, `moduleDetection: "force"`, `noEmit: true`, `jsx: "react-jsx"`, `strict: true`, `noUnusedLocals: true`, `noUnusedParameters: true`, `noFallthroughCasesInSwitch: true`, `noUncheckedIndexedAccess: true`
   - `include: ["src"]`

4. `packages/conductor-dashboard/tsconfig.node.json` — node config for vite.config.ts:
   - `compilerOptions`: `target: "ES2022"`, `lib: ["ES2023"]`, `module: "ESNext"`, `skipLibCheck: true`, `moduleResolution: "bundler"`, `allowImportingTsExtensions: true`, `isolatedModules: true`, `moduleDetection: "force"`, `noEmit: true`, `strict: true`
   - `include: ["vite.config.ts"]`

5. `packages/conductor-dashboard/vite.config.ts`:
   - Import `defineConfig` from `"vite"`, `react` from `"@vitejs/plugin-react"`, `tailwindcss` from `"@tailwindcss/vite"`
   - Export `defineConfig({ plugins: [react(), tailwindcss()] })`
   - Do NOT add PostCSS config (Tailwind v4 uses Vite plugin, not PostCSS)

6. `packages/conductor-dashboard/biome.json`:
   - Use research-provided config: recommended linter rules, space indent (width 2), line width 100, double quotes, semicolons always, trailing commas all, organize imports enabled

7. `packages/conductor-dashboard/index.html`:
   - Standard Vite index.html with `<div id="root">`, `<script type="module" src="/src/main.tsx">`
   - Title: "Conductor Dashboard"

8. `packages/conductor-dashboard/src/main.tsx`:
   - Import React, ReactDOM, App, and index.css
   - `ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><App /></React.StrictMode>)`

9. `packages/conductor-dashboard/src/App.tsx`:
   - Simple component: `function App() { return <div className="p-4"><h1 className="text-2xl font-bold">Conductor Dashboard</h1><p className="text-gray-600">Agent orchestration interface</p></div> }`
   - Export default

10. `packages/conductor-dashboard/src/index.css`:
    - `@import "tailwindcss";` (Tailwind v4 syntax -- NOT `@tailwind base/components/utilities`)

11. `packages/conductor-dashboard/src/vite-env.d.ts`:
    - `/// <reference types="vite/client" />`

After creating all files, run:
- `cd /home/huypham/code/digest/claude-auto && pnpm install` to install all dependencies
- `pnpm --filter conductor-dashboard exec biome check ./src` to verify Biome passes
- `pnpm --filter conductor-dashboard exec tsc --noEmit` to verify TypeScript passes
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto && pnpm install && pnpm --filter conductor-dashboard exec biome check ./src && pnpm --filter conductor-dashboard exec tsc --noEmit</automated>
  </verify>
  <done>Dashboard package installs cleanly. Biome check passes on all src files. TypeScript strict mode check passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow with parallel Python and Dashboard jobs</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with two parallel jobs (no `needs:` between them):

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python:
    name: Python checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Install dependencies
        run: uv sync --locked --dev
        working-directory: packages/conductor-core
      - name: Lint (ruff check)
        run: uv run ruff check .
        working-directory: packages/conductor-core
      - name: Format check (ruff format)
        run: uv run ruff format --check .
        working-directory: packages/conductor-core
      - name: Type check (pyright)
        run: uv run pyright
        working-directory: packages/conductor-core
      - name: Tests (pytest)
        run: uv run pytest
        working-directory: packages/conductor-core

  dashboard:
    name: Dashboard checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Biome check
        run: pnpm --filter conductor-dashboard exec biome check ./src
      - name: TypeScript check
        run: pnpm --filter conductor-dashboard exec tsc --noEmit
```

Notes:
- No `needs:` between python and dashboard jobs -- they run in parallel
- Python job uses `working-directory` per step (not job-level) for clarity
- Dashboard job uses `--frozen-lockfile` to prevent lockfile drift in CI
- Use `pnpm/action-setup@v4` with version 9 (match root package.json packageManager)
- Use `actions/setup-node@v4` with node 22 (current LTS)
- Use `astral-sh/setup-uv@v7` with cache enabled
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || python3 -c "import sys; print('YAML validation skipped - no PyYAML')"</automated>
  </verify>
  <done>CI workflow file exists at .github/workflows/ci.yml with two jobs (python, dashboard) that have no needs dependency (parallel). Python job runs ruff check, ruff format --check, pyright, pytest. Dashboard job runs biome check, tsc --noEmit.</done>
</task>

</tasks>

<verification>
- `pnpm install` succeeds at repo root
- `pnpm --filter conductor-dashboard exec biome check ./src` passes
- `pnpm --filter conductor-dashboard exec tsc --noEmit` passes
- `.github/workflows/ci.yml` exists with valid YAML and two parallel jobs
- `make lint` runs both Python and Dashboard checks (requires Plan 01 Python scaffold)
- `make test` runs pytest (requires Plan 01)
</verification>

<success_criteria>
A developer can run `pnpm install` at the repo root and have a working dashboard scaffold with Biome and TypeScript checks passing. The CI workflow is ready to run on push/PR to main with parallel Python and Dashboard jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md`
</output>
