---
phase: 43-agent-cell-widgets
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/conductor-core/tests/test_tui_agent_cells.py
  - packages/conductor-core/src/conductor/tui/widgets/transcript.py
autonomous: true
requirements:
  - ACELL-04

must_haves:
  truths:
    - "AgentCell mounts in the transcript showing agent name, role, and task title in a labeled badge header"
    - "AgentCell.update_status() transitions the cell display (working shimmer to waiting to done) without errors"
    - "AgentCell.finalize() works correctly whether or not streaming was ever started (defensive finalize)"
    - "OrchestratorStatusCell can be created, updated, and finalized as an ephemeral status cell"
    - "Multiple AgentCells with different agent_id values (including special chars) render independently with no CSS ID collisions"
  artifacts:
    - path: "packages/conductor-core/src/conductor/tui/widgets/transcript.py"
      provides: "AgentCell, OrchestratorStatusCell, _sanitize_id"
      contains: "class AgentCell"
    - path: "packages/conductor-core/tests/test_tui_agent_cells.py"
      provides: "Unit tests for all 5 success criteria"
      contains: "test_agent_cell_header_content"
  key_links:
    - from: "transcript.py AgentCell"
      to: "_SHIMMER_ON, _SHIMMER_OFF, _SHIMMER_INTERVAL, _SHIMMER_PERIOD, _ANIMATIONS"
      via: "module-level constants already in transcript.py"
      pattern: "_SHIMMER_ON|_SHIMMER_OFF|_ANIMATIONS"
    - from: "transcript.py AgentCell.__init__"
      to: "_sanitize_id()"
      via: "CSS ID sanitization to prevent DOM collisions with agent_monitor.py"
      pattern: "acell-.*_sanitize_id"
---

<objective>
Create AgentCell and OrchestratorStatusCell widget classes in transcript.py using TDD.

Purpose: These widget classes are the building blocks for all agent visibility in the transcript. Phase 44 mounts them via state.json events, Phase 45 wires OrchestratorStatusCell to SDK stream events. Without these classes, nothing downstream can render.

Output: Two new Widget subclasses in transcript.py, fully tested with 5 unit tests covering all ACELL-04 success criteria.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-agent-cell-widgets/43-RESEARCH.md
@.planning/phases/43-agent-cell-widgets/43-VALIDATION.md

@packages/conductor-core/src/conductor/tui/widgets/transcript.py

<interfaces>
<!-- Key types and constants the executor needs from transcript.py -->

From packages/conductor-core/src/conductor/tui/widgets/transcript.py:
```python
import math
import os
from textual.app import ComposeResult
from textual.color import Color
from textual.containers import VerticalScroll
from textual.timer import Timer
from textual.widgets import LoadingIndicator, Markdown, Static
from textual.widget import Widget

# Module-level constants (reuse for AgentCell shimmer):
_SHIMMER_ON = Color(150, 150, 255, 0.12)
_SHIMMER_OFF = Color(0, 0, 0, 0.0)
_SHIMMER_INTERVAL = 1.0 / 15  # ~15 fps
_SHIMMER_PERIOD = 1.4  # full cycle duration in seconds
_ANIMATIONS = os.environ.get("CONDUCTOR_NO_ANIMATIONS", "") not in ("1", "true", "yes")

# Existing classes: UserCell, AssistantCell, TranscriptPane
# New classes to add: _sanitize_id(), AgentCell, OrchestratorStatusCell
```

Test pattern (from tests/test_tui_agent_monitor.py):
```python
# Tests use inline run_test() — never in fixtures (Textual contextvars issue #4998)
async def test_example():
    from textual.app import App, ComposeResult

    class TestApp(App):
        def compose(self) -> ComposeResult:
            yield SomeWidget()

    app = TestApp()
    async with app.run_test() as pilot:
        widget = app.query_one(SomeWidget)
        assert widget is not None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for AgentCell and OrchestratorStatusCell</name>
  <files>packages/conductor-core/tests/test_tui_agent_cells.py</files>
  <behavior>
    - test_agent_cell_header_content: Mount AgentCell with agent_id="a1", agent_name="Alice", role="coder", task_title="Add auth module". Assert the cell-label Static contains "Alice", "coder", and "Add auth module".
    - test_agent_cell_update_status: Mount AgentCell, call update_status("waiting"), assert status-line text is "waiting". Call update_status("done"), assert status-line text is "done". Confirm no exceptions raised.
    - test_agent_cell_finalize_defensive: (a) Mount AgentCell, call finalize() immediately without any shimmer starting — assert no exception, status is "done". (b) Mount AgentCell, let it start shimmer (on_mount triggers it), then call finalize() — assert shimmer timer is None and status is "done".
    - test_orchestrator_status_cell_lifecycle: Mount OrchestratorStatusCell with label="Orchestrator — delegating", description="Planning tasks...". Assert label and body text rendered. Call update(description="Spawning 3 agents..."), assert body updated. Call finalize(), then call update(description="ignored") — assert body still shows pre-finalize text (finalize locks the cell).
    - test_multiple_agent_cells_no_id_collision: Mount 3 AgentCells with agent_ids containing special chars: "agent.uuid-1", "agent/uuid:2", "agent uuid 3". Assert all 3 mount without DuplicateIds error. Assert each has a unique widget id starting with "acell-". Assert all 3 can be queried independently.
  </behavior>
  <action>
    Create `packages/conductor-core/tests/test_tui_agent_cells.py` with 5 async test functions.

    Each test follows the inline run_test() pattern from existing TUI tests:
    1. Define a minimal TestApp class that composes the widget(s) under test
    2. Use `async with app.run_test() as pilot:` to mount
    3. Query widgets and assert expected behavior

    Set CONDUCTOR_NO_ANIMATIONS=1 in each test (or at module level) to disable shimmer/fade-in for deterministic assertions.

    Import AgentCell, OrchestratorStatusCell from conductor.tui.widgets.transcript (these don't exist yet — tests MUST fail with ImportError on first run, confirming RED phase).

    Do NOT create stub implementations. The tests must fail.
  </action>
  <verify>
    <automated>cd packages/conductor-core && CONDUCTOR_NO_ANIMATIONS=1 python -m pytest tests/test_tui_agent_cells.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 tests exist and fail with ImportError (AgentCell/OrchestratorStatusCell not yet defined). RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AgentCell, OrchestratorStatusCell, and _sanitize_id in transcript.py</name>
  <files>packages/conductor-core/src/conductor/tui/widgets/transcript.py</files>
  <behavior>
    - AgentCell.__init__ accepts agent_id, agent_name, role, task_title; sets widget id to f"acell-{_sanitize_id(agent_id)}"
    - AgentCell.compose yields cell-label Static with name/role/task, and status-line Static with "working..."
    - AgentCell.on_mount starts shimmer (with _ANIMATIONS guard for fade-in)
    - AgentCell.update_status(new_status) updates status-line text; stops shimmer if status != "working"; returns early if already finalized
    - AgentCell.finalize() stops shimmer, sets status to "done", updates status-line — idempotent, safe before compose
    - OrchestratorStatusCell.__init__ accepts label, description; compose yields both as Statics
    - OrchestratorStatusCell.update(label, description) updates respective Statics; no-op if finalized
    - OrchestratorStatusCell.finalize() sets _finalized=True, idempotent
    - _sanitize_id replaces non-alphanumeric with hyphens, strips leading/trailing hyphens, defaults to "agent"
  </behavior>
  <action>
    Add to `packages/conductor-core/src/conductor/tui/widgets/transcript.py` (AFTER the existing `AssistantCell` class and BEFORE `TranscriptPane`):

    1. Add `import re` at the top of the file (alongside existing imports).

    2. Add `_sanitize_id(agent_id: str) -> str` function: `re.sub(r"[^a-zA-Z0-9]", "-", agent_id).strip("-") or "agent"`.

    3. Add `AgentCell(Widget)` class following the exact pattern from 43-RESEARCH.md Pattern 1:
       - DEFAULT_CSS with $warning border-left (visually distinct from AssistantCell's $accent)
       - __init__ with agent_id, agent_name, role, task_title params; id=f"acell-{_sanitize_id(agent_id)}"
       - compose: cell-label Static with f"{name} [{role}] — {task_title}", status-line Static with "working..."
       - on_mount: _ANIMATIONS fade-in guard + _shimmer_forward()
       - update_status(new_status): guard `if self._status == "done": return`, update status-line, stop shimmer if not working
       - finalize(): _stop_shimmer(), set _status="done", update status-line — all guarded with try/except
       - _stop_shimmer(): stop timer if not None, set to None, reset tint to _SHIMMER_OFF
       - _shimmer_forward(): guard on status, set_interval timer
       - _shimmer_tick(): sine wave tint animation (copy from AssistantCell._shimmer_tick)

    4. Add `OrchestratorStatusCell(Widget)` class following 43-RESEARCH.md Pattern 2:
       - DEFAULT_CSS with $secondary border-left
       - __init__ with label, description params; _finalized=False
       - compose: orch-label Static, orch-body Static
       - on_mount: _ANIMATIONS fade-in guard
       - update(label, description): guard on _finalized, update Statics via query_one with try/except
       - finalize(): set _finalized=True

    Use $warning for AgentCell (amber/yellow — distinct from AssistantCell's $accent blue) and $secondary for OrchestratorStatusCell (green/teal). Phase 46 will refine colors.

    IMPORTANT: Use prefix "acell-" for AgentCell IDs (NOT "agent-" which collides with agent_monitor.py).
  </action>
  <verify>
    <automated>cd packages/conductor-core && CONDUCTOR_NO_ANIMATIONS=1 python -m pytest tests/test_tui_agent_cells.py -x -v</automated>
  </verify>
  <done>All 5 tests pass (GREEN). AgentCell and OrchestratorStatusCell are importable from transcript.py, render correctly, transition statuses, handle defensive finalize, and work with multiple concurrent instances with special-char IDs.</done>
</task>

</tasks>

<verification>
1. All 5 unit tests pass: `cd packages/conductor-core && CONDUCTOR_NO_ANIMATIONS=1 python -m pytest tests/test_tui_agent_cells.py -x -v`
2. Full test suite still passes (no regressions): `cd packages/conductor-core && python -m pytest tests/ -x`
3. AgentCell and OrchestratorStatusCell are importable: `cd packages/conductor-core && python -c "from conductor.tui.widgets.transcript import AgentCell, OrchestratorStatusCell; print('OK')"`
</verification>

<success_criteria>
- AgentCell renders labeled header with agent name, role, task title
- AgentCell.update_status() transitions working/waiting/done display
- AgentCell.finalize() is idempotent and stops shimmer timer
- OrchestratorStatusCell creates, updates, finalizes as ephemeral cell
- Multiple AgentCells with special-char IDs mount without CSS ID collision
- All 5 tests green, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/43-agent-cell-widgets/43-01-SUMMARY.md`
</output>
