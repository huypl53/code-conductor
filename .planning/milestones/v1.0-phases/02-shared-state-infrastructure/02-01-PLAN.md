---
phase: 02-shared-state-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/conductor-core/pyproject.toml
  - packages/conductor-core/src/conductor/state/__init__.py
  - packages/conductor-core/src/conductor/state/models.py
  - packages/conductor-core/src/conductor/state/errors.py
autonomous: true
requirements: [CORD-01, CORD-06]

must_haves:
  truths:
    - "Task, AgentRecord, Dependency, and ConductorState Pydantic models exist with all required fields"
    - "Enums TaskStatus and AgentStatus serialize to clean string values in JSON"
    - "ConductorState can be constructed empty (default factory) and serialized to valid JSON"
    - "StateError, StateLockTimeout, and StateCorrupted exception classes exist"
    - "The state package __init__.py re-exports all public types"
  artifacts:
    - path: "packages/conductor-core/src/conductor/state/models.py"
      provides: "Pydantic v2 models for ConductorState, Task, AgentRecord, Dependency, TaskStatus, AgentStatus"
      exports: ["ConductorState", "Task", "AgentRecord", "Dependency", "TaskStatus", "AgentStatus"]
    - path: "packages/conductor-core/src/conductor/state/errors.py"
      provides: "Custom exception hierarchy for state operations"
      exports: ["StateError", "StateLockTimeout", "StateCorrupted"]
    - path: "packages/conductor-core/src/conductor/state/__init__.py"
      provides: "Public API re-exports"
  key_links:
    - from: "packages/conductor-core/src/conductor/state/__init__.py"
      to: "models.py, errors.py"
      via: "re-export imports"
      pattern: "from conductor\\.state\\.models import"
---

<objective>
Add filelock and pydantic dependencies, then create the Pydantic v2 data models, enums, and error classes that define the shared state schema. This plan creates the contracts that Plan 02 (StateManager) implements against.

Purpose: Establish the typed schema for `.conductor/state.json` before any read/write logic exists. All downstream state operations depend on these types.
Output: `models.py` with all Pydantic models, `errors.py` with exception classes, updated `pyproject.toml` with new dependencies, and `__init__.py` re-exporting the public API.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-state-infrastructure/02-RESEARCH.md
@packages/conductor-core/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filelock and pydantic dependencies</name>
  <files>packages/conductor-core/pyproject.toml</files>
  <action>
Add `filelock>=3.16` and `pydantic>=2.10` to the `[project] dependencies` list in `packages/conductor-core/pyproject.toml`. Then run `cd packages/conductor-core && uv lock` to update the lockfile. Verify both packages resolve correctly.

Do NOT change any other fields in pyproject.toml. The dependencies list currently is `dependencies = []` — replace with the two new entries.
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run python -c "import filelock; import pydantic; print(f'filelock={filelock.__version__}, pydantic={pydantic.__version__}')"</automated>
  </verify>
  <done>filelock and pydantic are importable from the conductor-core package environment and lockfile is updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Pydantic models, enums, and error classes</name>
  <files>
    packages/conductor-core/src/conductor/state/models.py
    packages/conductor-core/src/conductor/state/errors.py
    packages/conductor-core/src/conductor/state/__init__.py
    packages/conductor-core/tests/test_models.py
  </files>
  <behavior>
    - Test: TaskStatus and AgentStatus enum values serialize as plain strings ("pending", not "TaskStatus.pending")
    - Test: Task model can be created with minimal fields (id, title, description) and defaults are populated (status=pending, assigned_agent=None, created_at/updated_at are UTC datetimes)
    - Test: AgentRecord model can be created with minimal fields (id, name, role) and defaults are populated (status=idle, current_task_id=None)
    - Test: Dependency model stores task_id and depends_on
    - Test: ConductorState can be created empty (no args) with version="1", empty lists for tasks/agents/dependencies
    - Test: ConductorState with tasks/agents/dependencies round-trips through model_dump_json() -> model_validate_json() with full fidelity
    - Test: StateError, StateLockTimeout, StateCorrupted can be raised and caught; StateLockTimeout and StateCorrupted inherit from StateError
  </behavior>
  <action>
Create `errors.py` with three exception classes:
- `StateError(Exception)` — base exception for all state operations
- `StateLockTimeout(StateError)` — raised when filelock times out
- `StateCorrupted(StateError)` — raised when state.json cannot be parsed

Create `models.py` following the exact schema from the research document (Pattern 2):
- `TaskStatus(StrEnum)`: PENDING, IN_PROGRESS, COMPLETED, FAILED, BLOCKED
- `AgentStatus(StrEnum)`: IDLE, WORKING, WAITING, DONE
- `Task(BaseModel)`: id, title, description, status, assigned_agent, outputs, created_at, updated_at — with `model_config = ConfigDict(use_enum_values=True)`
- `AgentRecord(BaseModel)`: id, name, role, current_task_id, status, registered_at — with `model_config = ConfigDict(use_enum_values=True)`
- `Dependency(BaseModel)`: task_id, depends_on
- `ConductorState(BaseModel)`: version="1", tasks list, agents list, dependencies list, updated_at — with `model_config = ConfigDict(use_enum_values=True)`

Use `from __future__ import annotations` at top. Use `datetime` with `timezone.utc` for all timestamp defaults. Use `Field(default_factory=...)` for mutable defaults.

Update `__init__.py` to re-export all public names: ConductorState, Task, AgentRecord, Dependency, TaskStatus, AgentStatus, StateError, StateLockTimeout, StateCorrupted.

Follow TDD: write tests first in `test_models.py`, verify they fail, then implement, verify they pass.
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run pytest tests/test_models.py -x -v</automated>
  </verify>
  <done>All model tests pass. Enum values serialize as clean strings. ConductorState round-trips through JSON. Error hierarchy is correct. All types importable from conductor.state.</done>
</task>

</tasks>

<verification>
- `cd packages/conductor-core && uv run pytest tests/test_models.py -x -v` — all model tests pass
- `cd packages/conductor-core && uv run ruff check src/conductor/state/` — no lint errors
- `cd packages/conductor-core && uv run pyright src/conductor/state/` — no type errors
- `cd packages/conductor-core && uv run python -c "from conductor.state import ConductorState, Task, AgentRecord, Dependency, TaskStatus, AgentStatus, StateError, StateLockTimeout, StateCorrupted; print('All exports OK')"` — all names importable
</verification>

<success_criteria>
- filelock and pydantic installed and importable
- All 6 Pydantic models/enums exist with correct fields and defaults
- 3 error classes with correct inheritance hierarchy
- JSON round-trip produces identical data (no enum repr leaks, no datetime loss)
- All exports available from `conductor.state`
- Ruff and pyright pass on all new files
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-state-infrastructure/02-01-SUMMARY.md`
</output>
