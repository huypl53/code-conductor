---
phase: 10-dashboard-frontend
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - packages/conductor-dashboard/src/components/AgentCard.tsx
  - packages/conductor-dashboard/src/components/AgentCard.test.tsx
  - packages/conductor-dashboard/src/components/LiveStream.tsx
  - packages/conductor-dashboard/src/components/LiveStream.test.tsx
  - packages/conductor-dashboard/src/components/InterventionPanel.tsx
  - packages/conductor-dashboard/src/components/InterventionPanel.test.tsx
  - packages/conductor-dashboard/src/components/NotificationProvider.tsx
  - packages/conductor-dashboard/src/components/NotificationProvider.test.tsx
  - packages/conductor-dashboard/src/App.tsx
  - packages/conductor-dashboard/src/main.tsx
autonomous: true
requirements: [DASH-02, DASH-03, DASH-06]
must_haves:
  truths:
    - "Expanding an agent card shows recent actions (events filtered by agent) and files modified (target_file, material_files)"
    - "Expanding further to stream level shows real-time DeltaEvents for that agent in a scrollable log"
    - "The developer can cancel an agent from the dashboard and the command is sent over WebSocket"
    - "The developer can send feedback to an agent from the dashboard and the command is sent over WebSocket"
    - "The developer can redirect an agent with new instructions from the dashboard"
    - "Smart notification events (task_completed, task_failed, intervention_needed) trigger toast notifications"
  artifacts:
    - path: "packages/conductor-dashboard/src/components/LiveStream.tsx"
      provides: "Real-time event stream view filtered by agent"
      exports: ["LiveStream"]
    - path: "packages/conductor-dashboard/src/components/InterventionPanel.tsx"
      provides: "Cancel, redirect, and feedback action buttons and forms"
      exports: ["InterventionPanel"]
    - path: "packages/conductor-dashboard/src/components/NotificationProvider.tsx"
      provides: "Sonner Toaster wrapper with smart notification hook"
      exports: ["NotificationProvider", "useSmartNotifications"]
  key_links:
    - from: "packages/conductor-dashboard/src/components/AgentCard.tsx"
      to: "packages/conductor-dashboard/src/components/LiveStream.tsx"
      via: "rendered when expansion level is 'stream'"
      pattern: "<LiveStream"
    - from: "packages/conductor-dashboard/src/components/AgentCard.tsx"
      to: "packages/conductor-dashboard/src/components/InterventionPanel.tsx"
      via: "rendered when expansion level is not 'collapsed'"
      pattern: "<InterventionPanel"
    - from: "packages/conductor-dashboard/src/components/InterventionPanel.tsx"
      to: "packages/conductor-dashboard/src/hooks/useConductorSocket.ts"
      via: "calls sendIntervention via onIntervene prop"
      pattern: "onIntervene.*action.*agent_id"
    - from: "packages/conductor-dashboard/src/components/NotificationProvider.tsx"
      to: "packages/conductor-dashboard/src/types/conductor.ts"
      via: "processes DeltaEvent[] for smart notifications"
      pattern: "is_smart_notification"
---

<objective>
Build the expanded detail view, live stream, intervention controls, and toast notifications. After this plan, the dashboard supports the full three-tier card expansion and agents can be cancelled, redirected, or given feedback directly from the browser.

Purpose: DASH-02 (expandable detail), DASH-03 (live stream), DASH-06 (interventions) -- the interactive layer.
Output: Complete dashboard with detail views, live event streaming, intervention panel, and smart toast notifications.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-dashboard-frontend/10-RESEARCH.md
@.planning/phases/10-dashboard-frontend/10-01-SUMMARY.md
@.planning/phases/10-dashboard-frontend/10-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (types) -->

From packages/conductor-dashboard/src/types/conductor.ts:
```typescript
export interface Task { id: string; title: string; description: string; status: TaskStatus; assigned_agent: string | null; target_file: string; material_files: string[]; /* ... */ }
export interface AgentRecord { id: string; name: string; role: string; current_task_id: string | null; status: AgentStatus; /* ... */ }
export interface DeltaEvent { type: EventType; task_id: string | null; agent_id: string | null; payload: Record<string, unknown>; is_smart_notification: boolean; }
export interface InterventionCommand { action: "cancel" | "redirect" | "feedback"; agent_id: string; message?: string; }
export type ExpansionLevel = "collapsed" | "detail" | "stream";
```

<!-- From Plan 02 (components and hook) -->

From packages/conductor-dashboard/src/hooks/useConductorSocket.ts:
```typescript
export function useConductorSocket(url: string): {
  conductorState: ConductorState | null;
  events: DeltaEvent[];
  connected: boolean;
  sendIntervention: (command: InterventionCommand) => void;
};
```

From packages/conductor-dashboard/src/components/AgentCard.tsx:
```typescript
// Props: { agent: AgentRecord; tasks: Task[]; events: DeltaEvent[]; onIntervene?: (command: InterventionCommand) => void }
// State: expansion: ExpansionLevel (default "collapsed")
// Detail section currently has placeholder text -- replace with real content
// Stream section currently has placeholder text -- replace with LiveStream component
```

From packages/conductor-dashboard/src/components/StatusBadge.tsx:
```typescript
export function StatusBadge({ status }: { status: AgentStatus }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LiveStream, InterventionPanel components, and AgentCard detail view</name>
  <files>
    packages/conductor-dashboard/src/components/LiveStream.tsx,
    packages/conductor-dashboard/src/components/LiveStream.test.tsx,
    packages/conductor-dashboard/src/components/InterventionPanel.tsx,
    packages/conductor-dashboard/src/components/InterventionPanel.test.tsx,
    packages/conductor-dashboard/src/components/AgentCard.tsx,
    packages/conductor-dashboard/src/components/AgentCard.test.tsx
  </files>
  <behavior>
    LiveStream:
    - Test: renders DeltaEvents filtered to the given agentId
    - Test: does not render events for other agents
    - Test: displays event type, task_id, and timestamp for each event
    - Test: auto-scrolls to bottom when new events arrive (ref on container)
    - Test: renders empty state when no events match agentId

    InterventionPanel:
    - Test: renders Cancel, Feedback, and Redirect buttons
    - Test: clicking Cancel calls onIntervene with {action: "cancel", agent_id}
    - Test: clicking Feedback opens a text input; submitting calls onIntervene with {action: "feedback", agent_id, message}
    - Test: clicking Redirect opens a text input; submitting calls onIntervene with {action: "redirect", agent_id, message}
    - Test: Feedback/Redirect inputs clear after submission
    - Test: Feedback/Redirect submit buttons are disabled when input is empty

    AgentCard detail view:
    - Test: detail level shows recent actions (last N events for this agent with human-readable descriptions)
    - Test: detail level shows files modified (target_file from current task, material_files)
    - Test: detail level shows InterventionPanel
    - Test: stream level shows LiveStream component
    - Test: stream level still shows InterventionPanel
  </behavior>
  <action>
    1. Create LiveStream component:
       - Props: { agentId: string; events: DeltaEvent[] }
       - Filter events where event.agent_id === agentId OR (event.task_id matches a task assigned to this agent -- pass this logic from parent or keep simple: just filter by agent_id)
       - Render each event as a row: timestamp-style index, event type as a badge (color-coded), task_id if present, payload summary
       - Use a scrollable container with max-h-64 overflow-y-auto
       - useRef + useEffect to scroll to bottom when events.length changes
       - Font: font-mono text-xs for log-like appearance
       - Empty state: "No events yet" in muted text
       - Tailwind: bg-gray-900 text-gray-100 for terminal-like look

    2. Create InterventionPanel component:
       - Props: { agentId: string; onIntervene: (command: InterventionCommand) => void }
       - Three action buttons in a row: Cancel (red), Feedback (blue), Redirect (amber)
       - Cancel: single click sends { action: "cancel", agent_id: agentId } immediately (with confirmation? No -- keep it fast, the backend handles gracefully)
       - Feedback: clicking toggles a text input below. Input has placeholder "Send feedback to agent...". Submit button sends { action: "feedback", agent_id: agentId, message }. Clear input after send. Submit disabled when empty.
       - Redirect: similar to Feedback but with placeholder "New instructions for agent...". Sends { action: "redirect", agent_id: agentId, message }.
       - Only one input open at a time (Feedback and Redirect are mutually exclusive)
       - Tailwind: flex gap-2 for buttons, transition-all for input appearance

    3. Update AgentCard to replace placeholder content:
       - Detail section (level !== "collapsed"):
         a. "Recent Actions" heading with last 10 events for this agent, rendered as human-readable lines: "[event.type] task: event.task_id" with relative time or index
         b. "Files" section: show current task's target_file and material_files (look up task from tasks array by agent.current_task_id). If no task, show "No files"
         c. Render <InterventionPanel agentId={agent.id} onIntervene={onIntervene} />
         d. "Show live stream" / "Hide live stream" toggle button
       - Stream section (level === "stream"):
         a. Render <LiveStream agentId={agent.id} events={events} />
         b. InterventionPanel still visible (above stream)

    4. Write all tests FIRST (red), then implement (green). For InterventionPanel tests, use userEvent for click and type interactions. For LiveStream, pass mock events and assert filtered rendering.
  </action>
  <verify>
    <automated>cd packages/conductor-dashboard && npx vitest run src/components/LiveStream.test.tsx src/components/InterventionPanel.test.tsx src/components/AgentCard.test.tsx</automated>
  </verify>
  <done>
    - LiveStream renders filtered events for a specific agent in a terminal-like scrollable log
    - InterventionPanel provides cancel/feedback/redirect actions that call onIntervene
    - AgentCard detail view shows recent actions, files modified, and intervention controls
    - AgentCard stream view shows LiveStream and intervention controls
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationProvider with Sonner toasts and App integration</name>
  <files>
    packages/conductor-dashboard/src/components/NotificationProvider.tsx,
    packages/conductor-dashboard/src/components/NotificationProvider.test.tsx,
    packages/conductor-dashboard/src/App.tsx,
    packages/conductor-dashboard/src/main.tsx
  </files>
  <behavior>
    useSmartNotifications:
    - Test: does not fire toast for non-smart events (is_smart_notification=false)
    - Test: fires toast.success for task_completed smart event
    - Test: fires toast.error for task_failed smart event
    - Test: fires toast.warning with duration=Infinity for intervention_needed smart event
    - Test: does not re-fire toasts for already-processed events (tracks lastProcessed index)

    App integration:
    - Test: App renders NotificationProvider (Toaster component present in DOM)
    - Test: App passes events to useSmartNotifications
  </behavior>
  <action>
    1. Create NotificationProvider component:
       - Wraps Sonner's <Toaster /> component with position="top-right"
       - Styled with Tailwind-compatible theme (default Sonner theme works)

    2. Create useSmartNotifications hook:
       - Takes events: DeltaEvent[]
       - useRef to track lastProcessed index (starts at 0)
       - useEffect: on events change, process events from lastProcessed to end
       - For each event where is_smart_notification === true:
         - task_completed: toast.success(`Task completed: ${event.task_id}`)
         - task_failed: toast.error(`Task failed: ${event.task_id}`)
         - intervention_needed: toast.warning(`Agent ${event.agent_id} needs intervention`, { duration: Infinity }) -- does not auto-dismiss
       - Update lastProcessed to events.length

    3. Update App.tsx:
       - Add <NotificationProvider /> (renders Toaster)
       - Call useSmartNotifications(events) in App component body
       - Ensure sendIntervention is passed through AgentGrid to AgentCard to InterventionPanel

    4. Update main.tsx if needed (likely no change -- just ensure StrictMode wraps App).

    5. Write tests FIRST. For toast tests, mock the `sonner` module's toast function (vi.mock("sonner")) and verify it's called with correct arguments. For App integration, render App with mocked useConductorSocket hook.
  </action>
  <verify>
    <automated>cd packages/conductor-dashboard && npx vitest run</automated>
  </verify>
  <done>
    - Smart notification events trigger appropriate toasts (success, error, warning)
    - Intervention-needed toasts persist until dismissed (duration: Infinity)
    - Non-smart events do not trigger toasts
    - Already-processed events are not re-fired
    - Full dashboard: cards + detail + stream + interventions + notifications all work together
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd packages/conductor-dashboard && npx vitest run` -- all tests green
2. `cd packages/conductor-dashboard && npx tsc -b --noEmit` -- TypeScript compiles clean
3. `cd packages/conductor-dashboard && npm run build` -- production build succeeds
</verification>

<success_criteria>
- Expanding a card shows recent actions, files modified, and intervention controls (DASH-02)
- Further expansion shows live event stream filtered by agent (DASH-03)
- Cancel, feedback, and redirect commands can be sent from the dashboard (DASH-06)
- Smart notification events trigger toast notifications
- Full test suite passes
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-frontend/10-03-SUMMARY.md`
</output>
