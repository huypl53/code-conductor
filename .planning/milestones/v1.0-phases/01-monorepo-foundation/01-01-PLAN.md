---
phase: 01-monorepo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - pnpm-workspace.yaml
  - package.json
  - Makefile
  - .gitignore
  - packages/conductor-core/pyproject.toml
  - packages/conductor-core/src/conductor/__init__.py
  - packages/conductor-core/src/conductor/cli/__init__.py
  - packages/conductor-core/src/conductor/state/__init__.py
  - packages/conductor-core/src/conductor/acp/__init__.py
  - packages/conductor-core/src/conductor/orchestrator/__init__.py
  - packages/conductor-core/tests/test_cli.py
autonomous: true
requirements:
  - PKG-03

must_haves:
  truths:
    - "uv sync in packages/conductor-core installs dependencies without errors"
    - "conductor --help runs and prints usage info without errors"
    - "Ruff lint and format check pass on the Python scaffold"
    - "Pyright type check passes on the Python scaffold"
    - "pytest runs and passes on the scaffold"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv virtual workspace root"
      contains: "package = false"
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace definition"
      contains: "packages/*"
    - path: "package.json"
      provides: "root npm scripts and workspace config"
      contains: "private"
    - path: "Makefile"
      provides: "unified dev commands"
      contains: "lint"
    - path: "packages/conductor-core/pyproject.toml"
      provides: "Python package config with CLI entry point, Ruff, Pyright, pytest"
      contains: "conductor.cli:main"
    - path: "packages/conductor-core/src/conductor/cli/__init__.py"
      provides: "CLI entry point"
      contains: "def main"
    - path: "packages/conductor-core/tests/test_cli.py"
      provides: "CLI smoke test"
      contains: "test_"
  key_links:
    - from: "packages/conductor-core/pyproject.toml"
      to: "packages/conductor-core/src/conductor/cli/__init__.py"
      via: "[project.scripts] conductor = conductor.cli:main"
      pattern: "conductor\\.cli:main"
    - from: "pyproject.toml"
      to: "packages/conductor-core"
      via: "uv workspace members glob"
      pattern: 'members.*packages'
---

<objective>
Create the monorepo root configuration and Python package scaffold with working CLI entry point.

Purpose: Establish the foundational project structure so that `uv sync` works, `conductor --help` runs, and all Python quality tools (Ruff, Pyright, pytest) pass on the scaffold.
Output: Root workspace configs (uv + pnpm), Makefile, and a complete Python package scaffold with CLI entry point and passing tests.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root configuration files</name>
  <files>pyproject.toml, pnpm-workspace.yaml, package.json, Makefile, .gitignore</files>
  <action>
Create five root-level files:

1. `pyproject.toml` — uv virtual workspace root:
   - `[project] name = "conductor-workspace"` (MUST differ from member name `conductor-core` to avoid uv name collision)
   - `version = "0.1.0"`, `requires-python = ">=3.12"`
   - `[tool.uv] package = false`
   - `[tool.uv.workspace] members = ["packages/*"]`
   - `[dependency-groups] dev = []`

2. `pnpm-workspace.yaml` — pnpm workspace definition:
   - `packages: ["packages/*"]`

3. `package.json` — root package.json (private):
   - `name: "conductor-monorepo"`, `private: true`
   - Scripts delegating to dashboard via `pnpm --filter conductor-dashboard`: `lint`, `typecheck`, `build`
   - `packageManager: "pnpm@9"` (check current stable pnpm 9.x version at runtime with `pnpm --version` and use exact version)

4. `Makefile` — unified dev commands:
   - `.PHONY: lint test build format`
   - `lint`: runs `cd packages/conductor-core && uv run ruff check . && uv run ruff format --check . && uv run pyright` then `pnpm --filter conductor-dashboard exec biome check ./src`
   - `test`: runs `cd packages/conductor-core && uv run pytest`
   - `build`: runs `pnpm --filter conductor-dashboard build`
   - `format`: runs `cd packages/conductor-core && uv run ruff format .` then `pnpm --filter conductor-dashboard exec biome format --write ./src`

5. `.gitignore` — add entries for:
   - Python: `__pycache__/`, `*.pyc`, `.venv/`, `*.egg-info/`, `dist/`, `.ruff_cache/`
   - Node: `node_modules/`, `dist/`
   - Runtime: `.conductor/`
   - IDE: `.vscode/`, `.idea/`
   - UV: `uv.lock` should NOT be gitignored (it's committed for reproducibility)
  </action>
  <verify>
    <automated>cat pyproject.toml && cat pnpm-workspace.yaml && cat package.json && cat Makefile && cat .gitignore</automated>
  </verify>
  <done>All five root files exist with correct content. pyproject.toml has name "conductor-workspace" and package=false. Makefile has lint/test/build targets.</done>
</task>

<task type="auto">
  <name>Task 2: Create Python package scaffold with CLI entry point and tests</name>
  <files>
    packages/conductor-core/pyproject.toml,
    packages/conductor-core/src/conductor/__init__.py,
    packages/conductor-core/src/conductor/cli/__init__.py,
    packages/conductor-core/src/conductor/state/__init__.py,
    packages/conductor-core/src/conductor/acp/__init__.py,
    packages/conductor-core/src/conductor/orchestrator/__init__.py,
    packages/conductor-core/tests/test_cli.py
  </files>
  <action>
Create the Python package structure:

1. `packages/conductor-core/pyproject.toml` — full package config:
   - `[project] name = "conductor-core"`, `version = "0.1.0"`, `requires-python = ">=3.12"`, `dependencies = []`
   - `[project.scripts] conductor = "conductor.cli:main"` — CLI entry point
   - `[build-system] requires = ["hatchling"]`, `build-backend = "hatchling.build"`
   - `[tool.hatch.build.targets.wheel] packages = ["src/conductor"]`
   - `[tool.ruff] target-version = "py312"`, `line-length = 88`
   - `[tool.ruff.lint] select = ["E", "F", "I", "B", "UP"]`
   - `[tool.ruff.format] quote-style = "double"`, `indent-style = "space"`
   - `[tool.pyright] include = ["src"]`, `pythonVersion = "3.12"`, `typeCheckingMode = "standard"`
   - `[tool.pytest.ini_options] testpaths = ["tests"]`, `pythonpath = ["src"]`, `addopts = ["--import-mode=importlib"]`
   - `[dependency-groups] dev = ["pytest>=8", "pyright>=1.1"]`

2. `packages/conductor-core/src/conductor/__init__.py`:
   - `"""Conductor: AI agent orchestration."""`
   - `__version__ = "0.1.0"`

3. `packages/conductor-core/src/conductor/cli/__init__.py`:
   - `"""Conductor CLI entry point."""`
   - `import argparse`
   - `def main() -> None:` function that creates an ArgumentParser with `prog="conductor"`, `description="Conductor: AI agent orchestration"`, calls `parser.parse_args()`
   - Print a message like "Conductor v0.1.0" when invoked with no args (after parse_args, print version and exit cleanly)

4. Domain subpackage `__init__.py` files (each with a module docstring only):
   - `packages/conductor-core/src/conductor/state/__init__.py` — `"""Conductor shared state management."""`
   - `packages/conductor-core/src/conductor/acp/__init__.py` — `"""Conductor ACP communication layer."""`
   - `packages/conductor-core/src/conductor/orchestrator/__init__.py` — `"""Conductor orchestrator agent."""`

5. `packages/conductor-core/tests/test_cli.py`:
   - `import subprocess`
   - `def test_conductor_help():` — runs `["uv", "run", "conductor", "--help"]` via subprocess, asserts returncode == 0, asserts "conductor" (case-insensitive) in stdout
   - `def test_conductor_version():` — imports `conductor` module, asserts `conductor.__version__` is a string

After creating all files, run:
- `cd packages/conductor-core && uv sync --dev` to install dependencies and create lockfile
- `cd packages/conductor-core && uv run ruff check .` to verify lint passes
- `cd packages/conductor-core && uv run ruff format --check .` to verify format passes
- `cd packages/conductor-core && uv run pyright` to verify type checking passes
- `cd packages/conductor-core && uv run pytest -x` to verify tests pass
- `cd packages/conductor-core && uv run conductor --help` to verify CLI works
  </action>
  <verify>
    <automated>cd packages/conductor-core && uv sync --dev && uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run pytest -x && uv run conductor --help</automated>
  </verify>
  <done>Python package installs cleanly via uv sync. All quality checks pass (ruff lint, ruff format, pyright). pytest runs and both tests pass. `conductor --help` prints usage without error.</done>
</task>

</tasks>

<verification>
- `uv sync` succeeds in packages/conductor-core
- `uv run conductor --help` prints usage text
- `uv run ruff check .` passes (from packages/conductor-core)
- `uv run ruff format --check .` passes
- `uv run pyright` passes
- `uv run pytest -x` passes (2 tests)
- Root Makefile `make test` and `make lint` work (after Plan 02 provides dashboard)
</verification>

<success_criteria>
A developer can clone the repo, run `cd packages/conductor-core && uv sync --dev`, and immediately have a working Python environment with a `conductor --help` CLI and all quality tools passing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-01-SUMMARY.md`
</output>
