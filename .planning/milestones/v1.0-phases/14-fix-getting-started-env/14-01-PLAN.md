---
phase: 14-fix-getting-started-env
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/GETTING-STARTED.md
autonomous: true
requirements:
  - PKG-04

must_haves:
  truths:
    - "Getting-started guide does not claim Conductor reads .env files"
    - "Getting-started guide provides correct shell export instructions for API key"
    - "Getting-started guide provides shell profile persistence guidance as replacement for .env convenience"
    - "A developer following only the guide encounters no incorrect instructions about environment configuration"
  artifacts:
    - path: "docs/GETTING-STARTED.md"
      provides: "Accurate getting-started documentation"
      contains: "export ANTHROPIC_API_KEY"
  key_links:
    - from: "docs/GETTING-STARTED.md (Configuration section)"
      to: "docs/GETTING-STARTED.md (Troubleshooting section)"
      via: "consistent API key setup guidance"
      pattern: "export ANTHROPIC_API_KEY"
---

<objective>
Remove false `.env` auto-loading claims from the getting-started guide and replace with accurate shell-based environment variable instructions.

Purpose: The guide currently tells users Conductor reads `.env` files (lines 51-57 and line 187), but no such functionality exists. A developer who follows this advice will create a `.env` file, skip the shell export, and hit "ANTHROPIC_API_KEY not set" errors. This fix makes the guide accurate.

Output: Updated `docs/GETTING-STARTED.md` with correct environment variable configuration instructions.
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-fix-getting-started-env/14-RESEARCH.md
@docs/GETTING-STARTED.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove false .env claims and add shell profile persistence guidance</name>
  <files>docs/GETTING-STARTED.md</files>
  <action>
Edit docs/GETTING-STARTED.md to fix two locations:

**Configuration section (lines 51-57):** Replace the current "Option 2: Use a `.env` file" block with shell profile persistence guidance. The result should be:

```markdown
**Option 1: Export in your shell (current session only)**

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
```

**Option 2: Add to your shell profile (persistent across sessions)**

Add the export to your `~/.bashrc` or `~/.zshrc`:

```bash
echo 'export ANTHROPIC_API_KEY="sk-ant-..."' >> ~/.bashrc
source ~/.bashrc
```
```

Note: Option 1 header changes from "Export in your shell" to "Export in your shell (current session only)" to clarify the difference from the new Option 2.

**Troubleshooting section (line 187):** Replace:
```
Or add it to a `.env` file in your project root. Verify with:
```
With:
```
You can also add it permanently to your shell profile (`~/.bashrc` or `~/.zshrc`). Verify with:
```

**After editing:** Search the entire file for any remaining `.env` references. The only acceptable `.env` mention would be if it appeared in an unrelated context (there should be none). Remove any other `.env` references found.

Do NOT add python-dotenv or any .env loading code. This is a documentation-only fix.
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto && grep -c "automatically reads.*\.env\|auto.*load.*\.env\|Use a \.env file" docs/GETTING-STARTED.md | xargs test 0 -eq && grep -q "export ANTHROPIC_API_KEY" docs/GETTING-STARTED.md && grep -q "shell profile" docs/GETTING-STARTED.md && echo "PASS"</automated>
  </verify>
  <done>
- Zero occurrences of "automatically reads .env" in the guide
- Zero occurrences of "Use a .env file" in the guide
- Configuration section has two options: shell export (temporary) and shell profile (persistent)
- Troubleshooting section recommends shell profile instead of .env
- No other .env references remain in the file
  </done>
</task>

</tasks>

<verification>
Full verification sweep:

```bash
# 1. No false .env claims remain
grep -n "\.env" docs/GETTING-STARTED.md
# Expected: zero lines (or only in unrelated context)

# 2. Correct guidance exists
grep -n "export ANTHROPIC_API_KEY" docs/GETTING-STARTED.md
# Expected: matches in Configuration and Troubleshooting sections

# 3. Shell profile persistence guidance exists
grep -n "shell profile\|bashrc\|zshrc" docs/GETTING-STARTED.md
# Expected: matches in both Configuration and Troubleshooting sections
```
</verification>

<success_criteria>
- The getting-started guide contains zero false claims about .env auto-loading
- A developer following the guide will correctly set their API key via shell export or shell profile
- Configuration and Troubleshooting sections are internally consistent
</success_criteria>

<output>
After completion, create `.planning/phases/14-fix-getting-started-env/14-01-SUMMARY.md`
</output>
