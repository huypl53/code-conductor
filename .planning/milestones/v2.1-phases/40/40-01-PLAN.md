---
phase: 40-borderless
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/conductor-core/tests/test_tui_borderless.py
  - packages/conductor-core/src/conductor/tui/widgets/transcript.py
  - packages/conductor-core/src/conductor/tui/widgets/command_input.py
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "CommandInput has no visible separator border-top — the input area blends into the layout"
    - "UserCell has a subtle single-cell left accent line, not a thick two-cell border"
    - "AssistantCell has a subtle single-cell left accent line, not a thick two-cell border"
    - "AgentMonitorPane column separator (border-left: solid $primary 20%) is unchanged"
    - "All three modal #dialog borders are unchanged"
  artifacts:
    - path: "packages/conductor-core/tests/test_tui_borderless.py"
      provides: "VIS-01 and VIS-02 regression tests"
      exports: ["test_command_input_no_border_top", "test_user_cell_subtle_border", "test_assistant_cell_subtle_border", "test_agent_monitor_retains_border_left", "test_modal_dialogs_retain_border"]
    - path: "packages/conductor-core/src/conductor/tui/widgets/transcript.py"
      provides: "Updated UserCell and AssistantCell DEFAULT_CSS with subtle accent lines"
      contains: "border-left: solid $primary 40%"
    - path: "packages/conductor-core/src/conductor/tui/widgets/command_input.py"
      provides: "CommandInput DEFAULT_CSS without border-top"
  key_links:
    - from: "UserCell DEFAULT_CSS"
      to: "border-left rendering"
      via: "Textual CSS engine"
      pattern: "border-left: solid \\$primary 40%"
    - from: "AssistantCell DEFAULT_CSS"
      to: "border-left rendering"
      via: "Textual CSS engine"
      pattern: "border-left: solid \\$accent 40%"
    - from: "CommandInput DEFAULT_CSS"
      to: "top edge rendering"
      via: "Textual CSS engine"
      pattern: "border-top absent"
---

<objective>
CSS-only borderless design: remove the CommandInput separator border-top and replace thick cell border-left markers with subtle solid accent lines at reduced opacity. AgentMonitorPane column separator and all modal borders are unchanged.

Purpose: Match Codex CLI's content-first aesthetic — structural layout chrome disappears, message role remains visually communicated by thin accent lines rather than thick box borders.
Output: 3 files modified — test scaffold (new), transcript.py (2 CSS lines changed), command_input.py (1 CSS line removed).
</objective>

<execution_context>
@/home/huypham/.claude/get-shit-done/workflows/execute-plan.md
@/home/huypham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key source files — read before modifying -->
@packages/conductor-core/src/conductor/tui/widgets/transcript.py
@packages/conductor-core/src/conductor/tui/widgets/command_input.py
@packages/conductor-core/src/conductor/tui/widgets/agent_monitor.py
@packages/conductor-core/src/conductor/tui/widgets/modals.py
@packages/conductor-core/src/conductor/tui/conductor.tcss

<interfaces>
<!-- Current border declarations being changed. Executor must match these exactly. -->

From packages/conductor-core/src/conductor/tui/widgets/transcript.py:

UserCell.DEFAULT_CSS (line 21-31):
```python
DEFAULT_CSS = """
UserCell {
    background: $primary 10%;
    border-left: thick $primary;    # CHANGE TO: border-left: solid $primary 40%;
    padding: 0 1;
    margin: 0 0 1 0;
}
...
"""
```

AssistantCell.DEFAULT_CSS (line 52-63):
```python
DEFAULT_CSS = """
AssistantCell {
    background: $surface;
    border-left: thick $accent;     # CHANGE TO: border-left: solid $accent 40%;
    padding: 0 1;
    margin: 0 0 1 0;
}
...
"""
```

From packages/conductor-core/src/conductor/tui/widgets/command_input.py:

CommandInput.DEFAULT_CSS (line 60-71):
```python
DEFAULT_CSS = """
CommandInput {
    height: 3;
    padding: 0 1;
    background: $panel;
    border-top: solid $primary 30%;   # REMOVE this line entirely
}
CommandInput Input {
    background: $panel;
    border: none;
}
"""
```

AgentMonitorPane.DEFAULT_CSS (agent_monitor.py, line 68-75) — DO NOT CHANGE:
```css
AgentMonitorPane {
    width: 30;
    height: 1fr;
    background: $panel;
    border-left: solid $primary 20%;   /* functional column separator — retain */
    padding: 1 1;
}
```

Modal #dialog borders (modals.py) — DO NOT CHANGE:
```css
FileApprovalModal #dialog {
    border: solid $primary;   /* intentional overlay chrome — retain */
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for VIS-01 and VIS-02</name>
  <files>packages/conductor-core/tests/test_tui_borderless.py</files>
  <behavior>
    - test_command_input_no_border_top: assert "border-top" not in CommandInput.DEFAULT_CSS
    - test_user_cell_subtle_border: assert "thick" not in UserCell.DEFAULT_CSS AND "border-left" in UserCell.DEFAULT_CSS AND "solid" in UserCell.DEFAULT_CSS
    - test_assistant_cell_subtle_border: assert "thick" not in AssistantCell.DEFAULT_CSS AND "border-left" in AssistantCell.DEFAULT_CSS AND "solid" in AssistantCell.DEFAULT_CSS
    - test_agent_monitor_retains_border_left: assert "border-left" in AgentMonitorPane.DEFAULT_CSS AND "solid $primary 20%" in AgentMonitorPane.DEFAULT_CSS
    - test_modal_dialogs_retain_border: assert "border: solid $primary" in FileApprovalModal.DEFAULT_CSS AND "border: solid $primary" in CommandApprovalModal.DEFAULT_CSS AND "border: solid $primary" in EscalationModal.DEFAULT_CSS
  </behavior>
  <action>
    Create tests/test_tui_borderless.py with exactly the tests listed in the behavior block.

    File header: `"""Phase 40: Borderless design — VIS-01 and VIS-02 CSS regression tests."""`

    Follow the project test pattern: pure function assertions on DEFAULT_CSS strings, no app mounting required. See test_tui_focus_altscreen.py for the file header pattern — no pytest fixtures, no class wrappers, no asyncio.

    All five tests are synchronous (def, not async def) — they only inspect class attributes.

    Run after creating the file to confirm all 5 tests FAIL (RED state):
    `cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run pytest tests/test_tui_borderless.py -x -v`

    Expected output: 2-3 tests fail (the ones checking "thick" not in DEFAULT_CSS and "border-top" not in DEFAULT_CSS). Tests for retain/modal may pass immediately — that is correct, they are guard tests.
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run pytest tests/test_tui_borderless.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    tests/test_tui_borderless.py exists with 5 test functions. At least test_command_input_no_border_top, test_user_cell_subtle_border, and test_assistant_cell_subtle_border FAIL (confirming RED state). Retain/modal guard tests may pass — that is correct.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VIS-01 and VIS-02 CSS changes</name>
  <files>
    packages/conductor-core/src/conductor/tui/widgets/transcript.py
    packages/conductor-core/src/conductor/tui/widgets/command_input.py
  </files>
  <behavior>
    - UserCell: border-left changes from `thick $primary` to `solid $primary 40%`
    - AssistantCell: border-left changes from `thick $accent` to `solid $accent 40%`
    - CommandInput: `border-top: solid $primary 30%;` line is removed; the rest of DEFAULT_CSS is unchanged
    - No other lines in either file are touched
    - AgentMonitorPane and modals.py are NOT modified
  </behavior>
  <action>
    Three targeted edits — one per changed line:

    **Edit 1 — transcript.py, UserCell.DEFAULT_CSS:**
    Replace:
    ```
        border-left: thick $primary;
    ```
    With:
    ```
        border-left: solid $primary 40%;
    ```

    **Edit 2 — transcript.py, AssistantCell.DEFAULT_CSS:**
    Replace:
    ```
        border-left: thick $accent;
    ```
    With:
    ```
        border-left: solid $accent 40%;
    ```

    **Edit 3 — command_input.py, CommandInput.DEFAULT_CSS:**
    Remove this line entirely (do not replace with anything — just delete it):
    ```
        border-top: solid $primary 30%;
    ```
    The CommandInput block should end up as:
    ```python
    CommandInput {
        height: 3;
        padding: 0 1;
        background: $panel;
    }
    ```

    Pitfall to avoid: Do NOT use `border: none;` as a replacement — that would add a new property. Simply delete the `border-top` line so CommandInput has no border declaration at all, which is the cleanest approach per research notes.

    After edits, run tests to confirm GREEN state:
    `cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run pytest tests/test_tui_borderless.py -v`
  </action>
  <verify>
    <automated>cd /home/huypham/code/digest/claude-auto/packages/conductor-core && uv run pytest tests/test_tui_borderless.py -v && uv run pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 5 tests in test_tui_borderless.py pass (GREEN). Full test suite passes. UserCell and AssistantCell DEFAULT_CSS contain `solid` and no longer contain `thick`. CommandInput DEFAULT_CSS does not contain `border-top`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `uv run pytest tests/test_tui_borderless.py -v` — all 5 pass
2. `uv run pytest tests/ -x -q` — full suite green
3. Grep confirms no `thick` border remains in transcript.py:
   `grep "border-left" packages/conductor-core/src/conductor/tui/widgets/transcript.py`
   Expected: `border-left: solid $primary 40%` and `border-left: solid $accent 40%`
4. Grep confirms border-top is gone from command_input.py:
   `grep "border" packages/conductor-core/src/conductor/tui/widgets/command_input.py`
   Expected: only `border: none` (the inner Input rule, unchanged)
5. AgentMonitorPane unchanged:
   `grep "border-left" packages/conductor-core/src/conductor/tui/widgets/agent_monitor.py`
   Expected: `border-left: solid $primary 20%`
</verification>

<success_criteria>
1. Screen, #app-body, and CommandInput have no visible box borders — CommandInput's border-top line is absent from DEFAULT_CSS
2. UserCell shows `border-left: solid $primary 40%` in DEFAULT_CSS — thinner, semi-transparent left accent line
3. AssistantCell shows `border-left: solid $accent 40%` in DEFAULT_CSS — thinner, semi-transparent left accent line
4. AgentMonitorPane retains `border-left: solid $primary 20%` — functional column separator unchanged
5. Modal #dialog borders unchanged — all three modal DEFAULT_CSS strings still contain `border: solid $primary`
6. All 5 tests in test_tui_borderless.py pass
7. Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/40-borderless/40-borderless-01-SUMMARY.md` with:
- What was built (3 edits: 2 in transcript.py, 1 removal in command_input.py)
- Files modified with exact line changes
- Test results (5/5 passing)
- Any observations about visual appearance for Phase 41 planning
</output>
